import { IdentificacionBiometrica } from './identificacion-biometrica';

describe('IdentificacionBiometrica', () => {
  it('Deberia crearse una instancia', () => {
    expect(new IdentificacionBiometrica()).toBeTruthy();
  });
});
