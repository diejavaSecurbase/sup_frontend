import { CamelCaseToEspacioPipe } from './camel-case-to-espacio.pipe';

describe('CamelCaseToEspacioPipe', () => {
  it('Deberia crearse una instancia', () => {
    const pipe = new CamelCaseToEspacioPipe();
    expect(pipe).toBeTruthy();
  });
});
